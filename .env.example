# Paid API calls are disabled unless explicitly enabled.
OPENAI_ENABLED=false
# Server only. Never use NEXT_PUBLIC_ for this key.
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-5.4
OPENAI_IMAGE_MODEL=gpt-image-2
