# GitHub最新版とjishingoto2の統合（2026-09-14）

対象は元デザイン版 `/Users/suzurinene/Codex感謝祭/jishingoto2`。`origin/main` の `0d7b75d`（PR #14・#15を含む10コミット）を取得し、元の `a67758e` からfast-forwardした後、ローカルの未コミット変更を再適用した。13ファイルの競合を解消し、ローカルの変更は未コミットのまま保持している。

## 取り込んだ変更

- `/` の子供／大人選択と `/home`。
- 5問の行動クイズ、揺れた後の場面、問題文を含む振り返り、新しい説明イラスト。
- 結果と予想図を1枚にするPNG保存、点数・アプリURL・任意の予想図添付によるSNS共有。
- 代表写真と画風参考画像による生成、失敗時のサンプル表示、開発中のメモリ状態保持。

## 両立させたローカル改善

- マスク済みの個別写真を最大3枚解析し、応答形式・根拠・分類・写真番号・囲み枠を検証する。
- 生成は家具確認後のクイズ開始時。動画では先頭の代表写真に写る確認済み対象だけを選び、一覧の座標をその写真内の座標に変換する。
- SNS共有のリンクは、取り込み元の公開先から利用中の `https://jishingoto.vercel.app` に合わせる。
- 結果・保存・共有は `aftermathInput()` で同じ写真と対象を使い、キャッシュを再利用する。写真や対象を変えた場合は再生成する。
- medium/JPEG、生成175秒＋比較25秒、一時的な拒否だけ最大1回再送、元写真との比較、経過時間表示、手動再試行を維持する。比較を完了できなかった場合は画面と保存画像に注記する。
- スマホ・タブレット縦横・分割画面に追従する本文幅、クイズと振り返りの2列配置、設定パネル、操作ボタン配置を維持する。

## 検証

- Nodeテスト115件、Lint、本番ビルド（`npm run build -- --webpack`）成功。
- 生成API成果物に `room-style-reference.jpeg` が含まれることを確認。
- 付属 `room-risk.jpg` と本棚・窓・出入口を使い、実APIを1回実行。37.7秒、JPEG data URL 332,115文字、元写真との比較はchecked。生成画像を目視し、元の窓・本棚・ドアの配置と床に落ちた本を確認。単一サンプルの測定であり、平均速度や実写の精度を示すものではない。
- 結果の振り返り・チェックリスト、画像不一致時の再試行、比較未完了の注記、共有時の再利用、家具の修正後の再確認と生成開始をブラウザで確認。
- 避難画面のモック体験をスマホ・タブレット縦横で最後まで操作できることを確認。
- 対象を絞ったブラウザ検証12ケースが成功。320／600／768／1024／1366pxでホーム・設定・振り返り・結果・クイズを確認。対象選択の切り替え、PNGのダウンロード完了（1080×1605）、任意の予想図添付、利用中の公開URLへの共有リンクも確認。
- 最終ビルドを `http://127.0.0.1:3001/` で起動し、そのURLで対象選択・保存・共有の操作テストを再確認した。

Google Mapsキーはこのフォルダに未設定。全ブラウザテストを試した際、APIモードのStreet Viewテストがキー未設定で失敗したため中断し、今回の統合箇所とキー不要のモック体験を検証した。実Google Maps・実機カメラ・外部SNSへの投稿はこの統合確認では実施していない。

## 復元用の退避

統合前の251ファイル、Git差分、チェックサムは `.git/codex-backups/20260914-171845/`。stash `e8349196fe781dfc812ce28449376b54c2726ff5` も保持している。`.env.local` と `.vercel/project.json` は統合前と同じチェックサム。jishingoto3への反映、GitHubへのpush、本番デプロイは今回の統合には含めていない。
